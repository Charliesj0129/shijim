[project]
name = "shijim"
version = "0.1.0"
description = "Shioaji-based market data ingestion, recorder, and CTA sandbox."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "shioaji",
    "pyzmq",
    "clickhouse-driver",
    "polars",
    "orjson",
]
